# samp-mcp is published to the public npm registry, so no scope-to-registry
# mapping is needed here. Publishing is driven by
# .github/workflows/publish.yml (NPM_TOKEN) and package.json#publishConfig.
